name: anderson_fitness
description: Aplicativo simples de treino offline

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^16.3.0
  camera: ^0.10.5+9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
